spring:
  config:
    import: secret.yml
  
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect:
    show-sql: true

server:
  port: 9091
